apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
      - name: my-app
        image: 058264270488.dkr.ecr.ca-central-1.amazonaws.com/webapp:1.0.1
        ports:
        - containerPort: 80
